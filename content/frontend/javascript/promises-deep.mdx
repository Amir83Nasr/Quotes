---
title: Promises Deep Dive
description: Promise states, chaining, static methods, error handling
order: 5
---

# Promises Deep Dive

<LearningObjectives
  objectives={[
    "Understand promise states (pending, fulfilled, rejected)",
    "Chain with .then, .catch, .finally",
    "Choose between all, allSettled, race, any",
    "Implement Promise.all from scratch",
  ]}
/>

## Promise States

Three states, settle once, one-way.

```javascript
new Promise((resolve, reject) => {
  /* pending */
  resolve("done") // → fulfilled
  // reject("err") // → rejected
})
new Promise((r) => {
  r("first")
  r("second")
}) // "first" — second ignored
```

## Chaining

Each .then returns new promise. Rejections skip to nearest .catch.

```javascript
fetch("/api/user/1")
  .then((r) => {
    if (!r.ok) throw Error("HTTP " + r.status)
    return r.json()
  })
  .then((user) => fetch("/api/posts?userId=" + user.id))
  .then((r) => r.json())
  .catch((err) => console.error(err))
  .finally(() => hideLoader())
```

<Alert>
  <AlertTriangle />
  <AlertTitle>Gotcha</AlertTitle>
  <AlertDescription>
    Put .catch before .finally. Errors in .finally aren't caught by .catch
    after.
  </AlertDescription>
</Alert>

## Static Methods

<Tabs>
  <TabsList>
    <TabsTrigger value="all">all</TabsTrigger>
    <TabsTrigger value="settled">allSettled</TabsTrigger>
    <TabsTrigger value="race">race</TabsTrigger>
    <TabsTrigger value="any">any</TabsTrigger>
  </TabsList>

  <TabsContent value="all">

```javascript
// Rejects on first rejection
const [users, posts] = await Promise.all([
  fetch("/api/users").then((r) => r.json()),
  fetch("/api/posts").then((r) => r.json()),
])
```

  </TabsContent>

  <TabsContent value="settled">

```javascript
// Never rejects — array of {status, value|reason}
const r = await Promise.allSettled([fetch("/api/a")])
```

  </TabsContent>

  <TabsContent value="race">

```javascript
// First settled wins (fulfill or reject)
await Promise.race([
  fetch(url),
  new Promise((_, rej) => setTimeout(() => rej("timeout"), 5000)),
])
```

  </TabsContent>

  <TabsContent value="any">

```javascript
// First fulfillment wins, only rejects if ALL reject
await Promise.any([fetch("/api/us-east"), fetch("/api/eu-west")])
```

  </TabsContent>
</Tabs>

## Promise.withResolvers()

External resolve/reject — clean for callback wrapping.

```javascript
const { promise, resolve, reject } = Promise.withResolvers()
fs.readFile("data.txt", "utf-8", (err, data) =>
  err ? reject(err) : resolve(data)
)
await promise
```

## Try It Yourself

See promise chaining in action — each `.then` receives the previous return value:

<LessonCodeExample
  code={`Promise.resolve(1)
  .then(x => x * 2)
  .then(x => x + 3)
  .then(x => \`Result: \${x}\`)
  .then(console.log)
  .catch(console.error)`}
  language="javascript"
  title="Promise Chaining"
  executable
  showLineNumbers
  explanation="Each `.then()` transforms the value and passes it to the next. Return a value (not a promise) and the next `.then()` receives it immediately. This is how you build sequential async pipelines."
/>

## Interactive Exercise

<LessonCodeExample
  mode="challenge"
  title="Implement Promise.all"
  description="Write `myPromiseAll(iterable)` that resolves with results array in order, rejects on first failure."
  code={`function myPromiseAll(iterable) {
  // Return single promise
  // Resolve with results array in input order
  // Reject immediately if any promise rejects
}`}
  language="javascript"
  hints={[
    "Use Promise.resolve() to wrap non-thenables",
    "Track results by index, use counter for completion",
  ]}
  previewType="mixed"
/>

## Lesson Summary

<LessonSummary
  points={[
    "Promises settle once: pending → fulfilled or rejected",
    ".then chains create new promises; .catch recovers; .finally cleans up",
    "all (fail-fast), allSettled (all results), race (first), any (first success)",
    "Promise.withResolvers() for clean external resolve/reject",
  ]}
/>
