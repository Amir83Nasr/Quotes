---
title: Animations & Transitions
description: Create smooth, performant animations with CSS transitions and keyframes
difficulty: intermediate
order: 9
---

<LearningObjectives
  objectives={[
    "Use CSS transitions for property interpolation on state changes",
    "Write @keyframes for multi-step animations",
    "Configure animation properties: duration, timing, iteration, direction",
    "Understand easing functions including cubic-bezier and steps()",
    "Apply transform and will-change for GPU-accelerated performance",
    "Handle prefers-reduced-motion for accessibility",
  ]}
/>

## CSS Transitions

Transitions interpolate between states when a property changes.

```css
.button {
  background: #6366f1;
  transform: scale(1);
  transition:
    background 0.3s ease,
    transform 0.2s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.button:hover {
  background: #4f46e5;
  transform: scale(1.05);
}
```

Shorthand: `transition: property duration easing delay;`

<Alert><AlertTitle>Transitionable Properties</AlertTitle><AlertDescription>Only interpolatable properties animate: colors, transforms, opacity, dimensions. `display: none` -> `block` won't transition. Use `opacity` + `visibility` for fade effects.</AlertDescription></Alert>

## @keyframes

Define multi-step animations with named keyframes.

```css
@keyframes slide-in {
  from {
    opacity: 0;
    transform: translateX(-100%);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes bounce {
  0%,
  100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-20px);
  }
}

.animated-card {
  animation:
    slide-in 0.5s ease-out,
    bounce 2s infinite 0.5s;
}
```

## Animation Properties

| Property                    | Purpose                          | Example       |
| --------------------------- | -------------------------------- | ------------- |
| `animation-name`            | Which @keyframes to use          | `slide-in`    |
| `animation-duration`        | Length of one cycle              | `0.5s`        |
| `animation-timing-function` | Easing curve                     | `ease-in-out` |
| `animation-iteration-count` | How many cycles                  | `infinite`    |
| `animation-direction`       | Play forward, reverse, alternate | `alternate`   |
| `animation-delay`           | Wait before starting             | `0.2s`        |
| `animation-fill-mode`       | State before/after animation     | `forwards`    |

```css
.spinner {
  animation: spin 1s linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}
```

## Easing Functions

```css
/* CSS named easings */
transition: transform 0.3s ease-in-out;

/* cubic-bezier(x1, y1, x2, y2) for custom curves */
transition: transform 0.5s cubic-bezier(0.68, -0.55, 0.27, 1.55);

/* steps(N) for discrete, frame-by-frame animation */
animation: typewriter 2s steps(10) forwards;
```

## Transform & will-change

`transform` animates on the GPU (composite layer). `will-change` hints the browser.

```css
.card {
  will-change: transform, opacity;
  transition:
    transform 0.3s,
    opacity 0.3s;
}

.card:hover {
  transform: translateY(-4px) scale(1.02);
  opacity: 0.9;
}
```

<Alert>
  <AlertTitle>will-change Sparingly</AlertTitle>
  <AlertDescription>
    Overuse creates memory pressure. Apply it right before animation starts,
    remove after. Or let the browser optimize on its own for simple transforms.
  </AlertDescription>
</Alert>

## Accessibility: prefers-reduced-motion

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

## See It In Action

Try creating a custom animation with CSS keyframes:

<LessonCodeExample
  code={`@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-20px); }
}
.pulse-box {
  background: #6366f1;
  color: white;
  padding: 1.5rem 2rem;
  border-radius: 8px;
  cursor: pointer;
  animation: bounce 0.6s ease infinite;
}`}
  language="css"
  title="Bounce Animation"
  showLineNumbers
  explanation="A `@keyframes bounce` rule defines a vertical bounce effect. Setting `animation: bounce 0.6s ease infinite` makes the element loop continuously. The animation applies to the `.pulse-box` selector, animating `translateY` between 0 and -20px."
/>

<LessonCodeExample
  title="Loading Spinner & Card Reveal"
  code={`<div class="container">
  <div class="spinner"></div>
  <div class="reveal-card">
    <h3>Content Loaded</h3>
    <p>This card should slide in after loading.</p>
  </div>
</div>`}
  language="html"
  explanation="Combines a CSS border-spinner with a card that slides in on load. The `.spinner` rotates via a `border-top-color` trick, while the `.reveal-card` demonstrates an entrance animation triggered after content is ready."
/>

<LessonSummary
  points={[
    "Transitions interpolate between states on property change",
    "@keyframes define multi-step animation sequences",
    "Use transform and opacity for GPU-accelerated animations",
    "cubic-bezier creates custom easing curves, steps() creates discrete frames",
    "Always provide a prefers-reduced-motion fallback",
  ]}
/>
