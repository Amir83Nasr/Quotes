---
title: Monorepos
description: Managing multiple packages in a single repository with pnpm and Turborepo
order: 10
---

# Monorepos

One repo, many packages. Share code, run tools across them, coordinate releases.

<LearningObjectives objectives={[
  "Understand monorepo vs multirepo tradeoffs",
  "Set up pnpm workspaces for package management",
  "Configure Turborepo for build orchestration",
  "Share TypeScript configs and packages across projects",
  "Manage dependencies across workspaces",
  "Decide when a monorepo makes sense"
]} />

## Monorepo vs Multirepo

<Tabs>
<TabsList>
<TabsTrigger value="mono">Monorepo</TabsTrigger>
<TabsTrigger value="multi">Multirepo</TabsTrigger>
</TabsList>
<TabsContent value="mono">

```text
my-project/
  packages/
    shared/       # Shared types, utils
    web/          # Next.js app
    api/          # Express server
    ui/           # Component library
  pnpm-workspace.yaml
  turbo.json
```
- Single CI pipeline
- Cross-package refactoring in one commit
- Shared tooling configs
- Atomic releases
</TabsContent>
<TabsContent value="multi">

```text
my-project/
  shared/        # Published as npm package
  web-app/       # Separate repo
  api-server/    # Separate repo
  ui-library/    # Separate repo
```
- Independent CI per repo
- Cross-repo changes = multiple PRs
- Each repo manages own deps
- Stricter boundaries
</TabsContent>
</Tabs>

| Aspect | Monorepo | Multirepo |
|--------|----------|-----------|
| Code sharing | Direct workspace import | npm published package |
| Refactoring | One commit across packages | Multiple PRs across repos |
| CI complexity | Smart caching needed | Simple per-repo |
| Ownership | Shared visibility | Clear boundaries |
| Build time | Can be parallelized with caching | Independent builds |

## pnpm Workspaces Setup

```yaml
# pnpm-workspace.yaml
packages:
  - "packages/*"
```

```json
// package.json (root)
{
  "name": "my-project",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
```

### Adding Packages

```bash
# Create packages
mkdir -p packages/shared packages/web packages/api

# Each gets its own package.json
# packages/shared/package.json
{
  "name": "@my-project/shared",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
```

```bash
# Install workspace dependency
pnpm add @my-project/shared --filter @my-project/web
# Links to workspace copy, not npm registry
```

## Shared Config Packages

```json
// packages/tsconfig/package.json
{
  "name": "@my-project/tsconfig",
  "version": "0.0.0",
  "files": ["base.json", "nextjs.json", "node.json"]
}
```

```json
// packages/tsconfig/base.json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  }
}
```

```json
// packages/web/tsconfig.json
{
  "extends": "@my-project/tsconfig/nextjs.json",
  "include": ["src"]
}
```

## Turborepo Build Orchestration

```json
// turbo.json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"], // deps build first
      "outputs": [".next/**", "dist/**"],
      "cache": true
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {},
    "test": {
      "dependsOn": ["build"]
    }
  }
}
```

```text
Without Turborepo:  web(45s) + api(30s) + shared(10s) = 85s sequential
With Turborepo:     shared(10s) → web(45s) + api(30s) in parallel = 55s
With caching:       Only changed packages rebuild
```

<Alert>
<AlertTitle>Turborepo Caching</AlertTitle>
<AlertDescription>Turborepo hashes inputs (source, deps, env vars). If hash matches a previous run, it restores outputs from cache instead of rebuilding. Configure outputs in turbo.json so cache knows what to save.</AlertDescription>
</Alert>

## Dependency Management

```bash
# List all local packages
pnpm list -r --depth 0

# Add dep to specific workspace
pnpm add react --filter @my-project/web

# Update all TypeScript across workspaces
pnpm up typescript --latest

# Check for circular deps
pnpm ls --graph

# Run command in all packages
pnpm -r run build
```

### Common Patterns

```json
// Root devDependencies — shared tooling
{
  "devDependencies": {
    "typescript": "^5.5.0",
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "turbo": "^2.0.0"
  }
}
```

## When Monorepo Makes Sense

**Good fit:**
- Multiple apps sharing UI components and types
- Backend + frontend in same ecosystem (e.g., all TypeScript)
- Coordinated releases across packages
- Small team (< 20) where cross-package visibility helps

**Bad fit:**
- Teams with independent release cycles
- Different tech stacks per package (Java + Node + Python)
- Giant repos causing slow CI (10k+ packages)
- Strict access control boundaries needed

## InteractiveExercise

Set up a basic workspace with a shared types package consumed by two apps.

<InteractiveExercise
  title="Basic Monorepo Setup"
  description="Create a pnpm workspace with shared types between a web app and API"
  language="ts"
  starterCode={`// Step 1: Create pnpm-workspace.yaml
// packages: ["packages/*"]

// Step 2: Create packages/shared/src/types.ts
export type User = {
  id: string
  name: string
  email: string
}

// Step 3: Create packages/shared/package.json
// name: "@my-project/shared"
// main: "./src/index.ts"
// types: "./src/index.ts"

// Step 4: Create packages/web/src/app.ts
// Import User type from @my-project/shared
// Display user name

// Step 5: Create packages/api/src/handler.ts
// Import User type from @my-project/shared
// Create a mock user list

// Step 6: Wire deps — pnpm add @my-project/shared --filter @my-project/web
// Step 7: Wire deps — pnpm add @my-project/shared --filter @my-project/api

// Write the implementation below:
// ──────────────────────────────────

// packages/shared/src/types.ts
export type User = {
  id: string
  name: string
  email: string
}

// packages/web/src/app.ts — TODO: import User and render

// packages/api/src/handler.ts — TODO: import User and return list

// Root package.json scripts:
// "dev": "turbo dev"
// "build": "turbo build"`}
  solutionCode={`// packages/shared/src/index.ts
export type User = {
  id: string
  name: string
  email: string
}

export type ApiResponse<T> = {
  data: T
  error: string | null
}

// packages/web/src/app.ts
import { User } from "@my-project/shared"

const users: User[] = [
  { id: "1", name: "Alice", email: "alice@example.com" },
  { id: "2", name: "Bob", email: "bob@example.com" },
]

export default function UserList() {
  return (
    <ul>
      {users.map((user) => (
        <li key={user.id}>
          {user.name} ({user.email})
        </li>
      ))}
    </ul>
  )
}

// packages/api/src/handler.ts
import { User, ApiResponse } from "@my-project/shared"

const users: User[] = [
  { id: "1", name: "Alice", email: "alice@example.com" },
]

export function getUsers(): ApiResponse<User[]> {
  return { data: users, error: null }
}

// pnpm-workspace.yaml
// packages:
//   - "packages/*"

// turbo.json
{
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "dev": { "cache": false, "persistent": true }
  }
}

// Usage:
// pnpm add @my-project/shared --filter @my-project/web
// pnpm add @my-project/shared --filter @my-project/api
// pnpm build  # Builds shared first, then web+api in parallel`}
  hints={[
    "pnpm-workspace.yaml declares which directories contain packages",
    "Each workspace package needs its own package.json with a name",
    "Use --filter to install a workspace dependency into another package",
    "Turborepo dependsOn: ['^build'] means 'build dependencies first'",
  ]}
/>

<LessonSummary points={[
  "pnpm workspaces link local packages without npm publish",
  "Turborepo caches build outputs — only rebuilds changed packages",
  "^build dependency means build upstream deps first",
  "Shared config packages (tsconfig, eslint) reduce duplication",
  "--filter scopes commands to specific workspace packages",
  "Monorepo works best: same ecosystem, shared code, coordinated releases",
  "Monorepo hurts: different stacks, independent teams, strict boundaries",
]} />
